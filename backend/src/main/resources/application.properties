spring.application.name=backend
spring.config.import=optional:file:.env[.properties]
spring.datasource.url=${DB_URL:jdbc:postgresql://aws-1-eu-west-1.pooler.supabase.com:5432/postgres?tcpKeepAlive=true}
spring.datasource.username=${DB_USERNAME:postgres.jhbqpzwaivcbxzgofjdz}
spring.datasource.password=${DB_PASSWORD}
spring.jpa.hibernate.ddl-auto=${HIBERNATE_DDL_AUTO:update}
spring.datasource.hikari.maximum-pool-size=${DB_MAXIMUM_POOL_SIZE:3}
spring.datasource.hikari.minimum-idle=${DB_MINIMUM_IDLE:1}
spring.datasource.hikari.connection-timeout=${DB_CONNECTION_TIMEOUT:20000}
jwt.secret=${JWT_SECRET}
jwt.expiration=${JWT_EXPIRATION:86400000}
spring.mail.host=${MAIL_HOST:sandbox.smtp.mailtrap.io}
spring.mail.port=${MAIL_PORT:2525}
spring.mail.username=${MAIL_USERNAME:0c598c3ece6f8e}
spring.mail.password=${MAIL_PASSWORD:579ebf6fc974f9}
spring.mail.properties.mail.smtp.auth=${MAIL_SMTP_AUTH:true}
spring.mail.properties.mail.smtp.starttls.enable=${MAIL_SMTP_STARTTLS_ENABLE:true}
spring.thymeleaf.cache=false
frontend.url=${FRONTEND_URL:http://localhost:5173}
supabase.url=${SUPABASE_URL}
supabase.service-role-key=${SUPABASE_SERVICE_ROLE_KEY}
supabase.storage.diplomas-bucket=${SUPABASE_STORAGE_BUCKET_DIPLOMAS:diplomas}
supabase.storage.diploma-max-bytes=${SUPABASE_DIPLOMA_MAX_BYTES:10485760}
supabase.storage.signed-url-ttl-seconds=${SUPABASE_SIGNED_URL_TTL_SECONDS:900}
